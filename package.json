{
  "name": "electron-snippet",
  "version": "1.0.0",
  "private": true,
  "description": "An Snippet editor build with electron and react. WIP",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package:mac": "electron-packager electron 'XML Viewer' --out=dist --asar --overwrite --icon=icon.icns"
  },
  "keywords": [],
  "author": "Jonas Faber",
  "devDependencies": {
    "electron-packager": "^8.1.0"
  }
}
